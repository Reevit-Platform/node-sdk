name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    name: Publish
    # GitHub-hosted (not Blacksmith): npm provenance via Sigstore only supports
    # github-hosted runners. Publish runs rarely, so it needn't be on Blacksmith.
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # mint the OIDC token npm trusts; enables provenance
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22   # OIDC trusted publishing requires Node >= 22.14.0
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Upgrade npm for OIDC trusted publishing
        run: npm install -g npm@11   # OIDC needs npm >= 11.5.1; Node 22 ships npm 10.x, so upgrade (npm@11 supports Node 22; npm@latest/12 needs Node >= 22.22.2)

      - name: Install dependencies
        run: npm ci

      - name: Audit runtime dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Typecheck
        run: npm run typecheck --if-present

      - name: Build
        run: npm run build

      - name: Verify checked-in dist is up to date
        run: |
          if [ -n "$(git ls-files 'dist/*' 'dist/**')" ]; then
            git diff --exit-code -- dist
          fi

      - name: Validate tracked package files
        run: |
          python3 - <<'PY'
          import pathlib, subprocess, sys
          root = pathlib.Path('.')
          tracked = subprocess.check_output(['git', 'ls-files', '-z'], text=True).split('\0')
          errors = []
          for rel in filter(None, tracked):
              if rel.endswith('.gitkeep'):
                  continue
              path = root / rel
              if rel.endswith(('.new', '.tmp', '.bak', '.orig')):
                  errors.append(f"tracked temp file: {rel}")
              if path.is_file() and path.stat().st_size == 0:
                  errors.append(f"empty tracked file: {rel}")
          if errors:
              print('\n'.join(errors))
              sys.exit(1)
          PY

      - name: Publish to npm
        run: npm publish --access public
        # Authenticated via OIDC trusted publishing (no NODE_AUTH_TOKEN);
        # npm attaches build provenance automatically. Requires a Trusted
        # Publisher configured for @reevit/node on npmjs.com.
